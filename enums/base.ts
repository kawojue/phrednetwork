export enum Token {
    email = 'email',
    password = 'password'
}

export enum Article {
    all = "all",
    pending = "pending",
    active = "active",
}

export enum DonationAndAdverts {
    active = "active",
    expired = "expired"
}

export enum AdminitorRole {
    admin = 'admin',
    auditor = 'auditor'
}

export enum GlobalSearch {
    jobs = "jobs",
    posts = "posts",
    people = "people",
    forums = "forums",
}

export enum BooleanString {
    true = "true",
    false = "false"
}

export enum Action {
    accept = "accept",
    decline = "decline"
}

export enum Tab {
    for_you = "for_you",
    following = "following"
}